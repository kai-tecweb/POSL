# OpenAI API設定
OPENAI_API_KEY=your_openai_api_key_here

# X (Twitter) API設定
X_API_KEY=your_x_api_key_here
X_API_SECRET=your_x_api_secret_here
X_ACCESS_TOKEN=your_x_access_token_here
X_ACCESS_TOKEN_SECRET=your_x_access_token_secret_here

# AWS設定（本番環境用）
AWS_REGION=ap-northeast-1
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here

# 開発環境設定
NODE_ENV=development
API_URL=http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3001

# ローカルAWSサービス設定
AWS_ENDPOINT_URL=http://localhost:8000
S3_ENDPOINT_URL=http://localhost:9000
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123

# MySQL設定（移行対応）
MYSQL_HOST=localhost
MYSQL_PORT=3307
MYSQL_USER=posl_user
MYSQL_PASSWORD=posl_password
MYSQL_DATABASE=posl_db

# 追加API設定（Phase 7完了対応）
# Google Trends API（PyTrends - Python実行のため認証不要）
GOOGLE_TRENDS_ENABLED=true

# Yahoo Trends API（モックデータ生成）
YAHOO_TRENDS_ENABLED=true

# OpenAI Whisper API（OpenAI APIキーと共通）
WHISPER_ENABLED=true

# 自動投稿設定
AUTO_POST_ENABLED=true
CRON_SCHEDULE=0 11 * * *
POST_ENVIRONMENT=development

# AWS Services（本番環境用）
AWS_S3_BUCKET_NAME=posl-audio-files-dev
AWS_CLOUDWATCH_LOG_GROUP=posl-logs-dev

# Phase 7新機能フラグ
X_API_ENABLED=true
TRENDS_API_ENABLED=true
VOICE_DIARY_ENABLED=true
SCHEDULER_ENABLED=true

# パフォーマンス設定
OPENAI_TIMEOUT=30000
X_API_TIMEOUT=10000
MYSQL_CONNECTION_LIMIT=10

# 開発・デバッグ用設定
DEBUG_MODE=true
LOG_LEVEL=info

# -------------------------
# 以下覚書
# -------------------------


# -------------------------
# AWS 共通
# -------------------------
AWS_DEFAULT_REGION=ap-northeast-1
AWS_REGION=ap-northeast-1

# ローカル 用 IAMユーザーのキー
AWS_ACCESS_KEY_ID=（ローカル or CI 用アクセスキー）
AWS_SECRET_ACCESS_KEY=（ローカル or CI 用シークレットキー）
# GitHub Actions 用 IAMユーザーのキー
AWS_ACCESS_KEY_ID=（ローカル or CI 用アクセスキー）
AWS_SECRET_ACCESS_KEY=（ローカル or CI 用シークレットキー）

# -------------------------
# S3
# -------------------------
AWS_S3_BUCKET=（使うバケット名）
AWS_S3_UPLOAD_PATH=uploads
AWS_S3_ENDPOINT_URL=https://s3.ap-northeast-1.amazonaws.com

# -------------------------
# RDS（MySQL例）
# -------------------------
DB_CONNECTION=mysql
DB_HOST=（RDSエンドポイント）
DB_PORT=3306
DB_DATABASE=（DB名）
DB_USERNAME=（DBユーザー名）
DB_PASSWORD=（DBパスワード）